// Humans = the managing team. This is the full team-management view (invite,
// roles, deactivate, last active) — the same UserManagement screen, surfaced as
// its own top-level "Humans" nav item under Agents.
export { default } from '../settings/users/page'
